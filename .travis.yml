language: ruby

rvm: 2.2

cache: bundler

before_script: gem install awesome_bot

script: awesome_bot --allow-dupe --allow-redirect *.md

notifications:
  email:
    on_success: never
    on_failure: always
